apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cafeteria-keycloak-pvc
  namespace: cafeteria
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 2Gi

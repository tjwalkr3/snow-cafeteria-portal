apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cafeteria-db-pvc
  namespace: cafeteria
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 5Gi
